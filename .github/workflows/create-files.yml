name: 📂 Create Portfolio Structure

on:
  workflow_dispatch:

jobs:
  build:
    # ADD THIS BLOCK ⬇️
    permissions:
      contents: write # Grants the GITHUB_TOKEN write permission for files
    # END OF BLOCK ⬆️
    runs-on: ubuntu-latest

    steps:
    - name: ⬇️ Checkout Repository
      uses: actions/checkout@v4

    # ... (other steps here) ...

    - name: ⬆️ Commit and Push Changes
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: 'feat: Initial creation of portfolio file structure via GitHub Actions'
        file_pattern: 'devops-portfolio/**'
